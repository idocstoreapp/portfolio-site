---
import Layout from "../../layouts/Layout.astro";
import ProjectHero from "../../components/ProjectHero.astro";
import SuccessCaseCard from "../../components/SuccessCaseCard.astro";
import TestimonialCardPremium from "../../components/TestimonialCardPremium.astro";

const projectData = {
  title: ["Gourmet Árabe", "Sistema de Menú QR"],
  description: "Sistema digital de menú QR para restaurante árabe, permitiendo a los clientes ver el menú desde sus dispositivos móviles de forma moderna e intuitiva.",
  mockup: "/images/menuqr/mockup-arabe-Photoroom.png",
  liveUrl: "https://sistema-restaurant-seven.vercel.app/",
  githubUrl: "https://github.com/idocstoreapp/sistema-restaurant",
  problem: "Necesidad de modernizar la experiencia del cliente eliminando menús físicos, mejorando la higiene y facilitando la actualización del menú en tiempo real.",
  solution: "Sistema de menú digital con código QR, diseño atractivo que refleja la identidad árabe, fácil navegación y actualización instantánea de precios y platos.",
  result: "Reducción de costos en menús físicos, mejor experiencia del cliente, actualización fácil del menú y profesionalismo en la atención.",
  testimonial: {
    name: "Gourmet Árabe",
    role: "Propietarios",
    company: "Gourmet Árabe",
    rating: 5,
    text: "El sistema de menú QR ha transformado la experiencia en nuestro restaurante. Los clientes pueden ver nuestro menú completo desde sus celulares, es muy moderno y profesional. Además, podemos actualizar precios y platos al instante sin imprimir nuevos menús.",
    avatar: "🍽️"
  },
  stack: ["Astro", "React", "Tailwind CSS", "TypeScript"]
};
---

<Layout 
  title={`${projectData.title.join(' ')} | Desarrollo Web | Jonathan Guarirapa`}
  description={projectData.description}
  image={projectData.mockup}
>
  <ProjectHero
    title={projectData.title}
    description={projectData.description}
    mockup={projectData.mockup}
    liveUrl={projectData.liveUrl}
    githubUrl={projectData.githubUrl}
  />

  <!-- Problem → Solution → Result -->
  <section class="section-spacing bg-gradient-dark">
    <div class="container">
      <h2 class="section-title text-center mb-12">El Proceso</h2>
      <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
        <SuccessCaseCard
          title="El Problema"
          problem={projectData.problem}
          solution=""
          result=""
          icon="📄"
        />
        <SuccessCaseCard
          title="La Solución"
          problem=""
          solution={projectData.solution}
          result=""
          icon="💡"
        />
        <SuccessCaseCard
          title="El Resultado"
          problem=""
          solution=""
          result={projectData.result}
          icon="✅"
        />
      </div>
    </div>
  </section>

  <!-- Client Testimonial -->
  <section class="section-spacing bg-gradient-darker">
    <div class="container">
      <h2 class="section-title text-center mb-12">Reseña del Cliente</h2>
      <div class="max-w-3xl mx-auto">
        <TestimonialCardPremium
          name={projectData.testimonial.name}
          role={projectData.testimonial.role}
          company={projectData.testimonial.company}
          rating={projectData.testimonial.rating}
          text={projectData.testimonial.text}
          avatar={projectData.testimonial.avatar}
          projectCover={projectData.mockup}
        />
      </div>
    </div>
  </section>

  <!-- Tech Stack -->
  <section class="section-spacing bg-gradient-dark">
    <div class="container">
      <h2 class="section-title text-center mb-8">Tecnologías Utilizadas</h2>
      <div class="flex flex-wrap justify-center gap-4">
        {projectData.stack.map((tech) => (
          <span class="tech-badge">
            {tech}
          </span>
        ))}
      </div>
    </div>
  </section>

  <!-- Back to Projects -->
  <section class="section-spacing text-center bg-gradient-darker">
    <div class="container">
      <a href="/projects" class="btn-back">
        ← Volver a Proyectos
      </a>
    </div>
  </section>

  <style>

    .tech-badge {
      padding: 0.5rem 1rem;
      background: rgba(88, 101, 242, 0.15);
      color: var(--accent-primary);
      border-radius: var(--radius-full);
      font-size: 0.875rem;
      font-weight: 600;
      border: 1px solid rgba(88, 101, 242, 0.3);
    }

    .section-spacing {
      padding: 4rem 0;
    }

    .container {
      max-width: 1400px;
      margin: 0 auto;
      padding: 0 1rem;
    }

    .section-title {
      font-size: clamp(2rem, 4vw, 2.5rem);
      font-weight: 700;
      color: var(--color-graphite-dark, #2B2B2B);
      font-family: 'Libre Baskerville', serif; /* Misma fuente que el hero */
    }
    
    .bg-gradient-dark .section-title,
    .bg-gradient-darker .section-title {
      color: var(--color-graphite-dark, #2B2B2B) !important;
    }

    .btn-back {
      display: inline-flex;
      align-items: center;
      gap: 0.5rem;
      padding: 1rem 2rem;
      background: rgba(255, 255, 255, 0.1);
      color: var(--text-primary);
      border: 1px solid rgba(255, 255, 255, 0.2);
      border-radius: var(--radius-md);
      font-weight: 600;
      text-decoration: none;
      transition: all var(--norm) var(--ease);
      font-size: 1.1rem;
    }

    .btn-back:hover {
      background: rgba(255, 255, 255, 0.2);
      transform: translateY(-2px);
    }
  </style>
</Layout>

