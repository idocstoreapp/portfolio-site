---
import Layout from '../layouts/Layout.astro';
import Button from '../components/Button.astro';
import { getCollection } from 'astro:content';

// Get featured projects for the homepage
const allProjects = await getCollection('projects');
const featuredProjects = allProjects
  .filter(project => project.data.featured)
  .sort((a, b) => b.data.order - a.data.order)
  .slice(0, 4);

// Testimonials data
const testimonials = [
  {
    name: "José Perez",
    role: "Director",
    company: "NubeX",
    rating: 5,
    text: "Gracias a Jonathan, nuestra plataforma aumentó un 40% en usuarios activos. El sistema es robusto y fácil de usar.",
    avatar: "👨‍💼"
  },
  {
    name: "Ana Torres",
    role: "CEO",
    company: "El Gabriel",
    rating: 5,
    text: "Excelente trabajo en nuestra tienda online. Las ventas aumentaron significativamente y la experiencia de usuario es impecable.",
    avatar: "👩‍💼"
  },
  {
    name: "Inés Traba",
    role: "CEO",
    company: "Mi Life",
    rating: 5,
    text: "Problema: Mi App tenía baja retención. Solución: Plataforma completa que aumentó usuarios activos en 50%.",
    avatar: "👩‍💻"
  },
  {
    name: "Anna Tellez",
    role: "Fundadora",
    company: "Bloom",
    rating: 5,
    text: "Sistema de automatización que redujo costos en 30%. Muy profesional y resultados medibles.",
    avatar: "👩‍🎨"
  }
];
---

<Layout 
  title="Diseñador Web y Programador | Jonathan Guarirapa - Desarrollo Web, Apps y Automatizaciones"
  description="Diseñador web profesional especializado en diseño de páginas web, apps web, diseño de marca, logos, menús QR para restaurantes y sistemas de gestión. Desarrollo web con resultados medibles."
  image="/jonadevel.jpg"
>
  <!-- Structured Data for Homepage -->
  <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "WebSite",
    "name": "Jonathan Guarirapa - Diseñador Web y Programador",
    "url": Astro.url.origin,
    "description": "Diseñador web profesional especializado en diseño de páginas web, apps web, diseño de marca, logos, menús QR para restaurantes y sistemas de gestión",
    "author": {
      "@type": "Person",
      "name": "Jonathan Guarirapa",
      "email": "jonacrd@gmail.com"
    },
    "potentialAction": {
      "@type": "SearchAction",
      "target": {
        "@type": "EntryPoint",
        "urlTemplate": Astro.url.origin + "/proyectos?q={search_term_string}"
      },
      "query-input": "required name=search_term_string"
    },
    "mainEntity": {
      "@type": "Person",
      "name": "Jonathan Guarirapa",
      "jobTitle": "Diseñador Web y Programador",
      "description": "Especialista en diseño de páginas web, diseño de apps web, diseño de marca, diseño de logos, creación de imágenes, menús QR para restaurantes y sistemas de gestión",
      "knowsAbout": [
        "Diseño Web",
        "Diseño de Páginas Web",
        "Diseño de Apps Web",
        "Diseño de Marca",
        "Diseño de Logo",
        "Creación de Imágenes",
        "Menús QR para Restaurantes",
        "Menú QR",
        "Sistemas de Gestión",
        "Programación",
        "Desarrollo Web",
        "Aplicaciones Web",
        "Automatizaciones"
      ],
      "offers": [
        {
          "@type": "Offer",
          "itemOffered": {
            "@type": "Service",
            "name": "Diseño de Páginas Web",
            "description": "Diseño profesional de páginas web modernas y responsivas"
          }
        },
        {
          "@type": "Offer",
          "itemOffered": {
            "@type": "Service",
            "name": "Diseño de Apps Web",
            "description": "Desarrollo de aplicaciones web personalizadas"
          }
        },
        {
          "@type": "Offer",
          "itemOffered": {
            "@type": "Service",
            "name": "Diseño de Marca y Logo",
            "description": "Creación de identidad visual y diseño de logos profesionales"
          }
        },
        {
          "@type": "Offer",
          "itemOffered": {
            "@type": "Service",
            "name": "Menús QR para Restaurantes",
            "description": "Sistemas de menús digitales con código QR para restaurantes"
          }
        },
        {
          "@type": "Offer",
          "itemOffered": {
            "@type": "Service",
            "name": "Sistemas de Gestión",
            "description": "Desarrollo de sistemas de gestión personalizados para empresas"
          }
        }
      ]
    }
  })} />
  <!-- Hero Section with Device Mockups -->
  <section class="hero-section">
    <div class="hero-background"></div>
    
    <div class="hero-container">
      <div class="hero-content">
        <!-- Header with Logo -->
        <div class="hero-header">
          <div class="hero-logo">
            <div class="logo-icon">JG</div>
            <span class="logo-text">Jonathan Guarirapa</span>
          </div>
        </div>

        <!-- Main Hero Content -->
        <div class="hero-main">
          <h1 class="hero-title">
            Diseñador Web y Programador | 
            <span class="gradient-text">Diseño de Páginas Web, Apps, Marca y Menús QR</span>
          </h1>
          
          <p class="hero-subtitle">
            Especialista en diseño web, diseño de páginas web, apps web, diseño de marca, logos, menús QR para restaurantes y sistemas de gestión. Soluciones reales con resultados medibles.
          </p>

          <!-- Ratings & Reviews -->
          <div class="hero-ratings">
            <div class="rating-stars">
              <span class="star">⭐</span>
              <span class="star">⭐</span>
              <span class="star">⭐</span>
              <span class="star">⭐</span>
              <span class="star">⭐</span>
            </div>
            <div class="rating-text">
              <span class="rating-score">4.9/5.0</span>
              <span class="rating-separator">•</span>
              <span class="rating-count">250+ Reseñas en Google</span>
            </div>
          </div>

          <!-- CTA Buttons -->
          <div class="hero-cta">
            <a href="/projects" class="btn btn-primary">
              Ver Portafolio
              <svg class="btn-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
              </svg>
            </a>
            <a href="/contacto" class="btn btn-secondary">
              Solicitar Servicio
            </a>
          </div>
        </div>

        <!-- Device Mockups -->
        <div class="hero-mockups">
          <div class="mockup-phone mockup-phone-1">
            <div class="phone-frame">
              <div class="phone-notch"></div>
              <div class="phone-screen">
                {featuredProjects[0] ? (
                  <img 
                    src={featuredProjects[0].data.cover || "/images/projects/tienda-online/cover.svg"} 
                    alt={featuredProjects[0].data.title}
                    class="phone-content"
                  />
                ) : (
                  <div class="phone-placeholder">
                    <div class="phone-app-grid">
                      <div class="phone-app-icon" style="background: #3b82f6;"></div>
                      <div class="phone-app-icon" style="background: #7c4dff;"></div>
                      <div class="phone-app-icon" style="background: #10b981;"></div>
                      <div class="phone-app-icon" style="background: #6b7280;"></div>
                    </div>
                  </div>
                )}
              </div>
            </div>
          </div>
          <div class="mockup-phone mockup-phone-2">
            <div class="phone-frame">
              <div class="phone-notch"></div>
              <div class="phone-screen">
                {featuredProjects[1] ? (
                  <img 
                    src={featuredProjects[1].data.cover || "/images/projects/landing-barberia/cover.svg"} 
                    alt={featuredProjects[1].data.title}
                    class="phone-content"
                  />
                ) : (
                  <div class="phone-placeholder">
                    <div class="phone-app-grid">
                      <div class="phone-app-icon" style="background: #3b82f6;"></div>
                      <div class="phone-app-icon" style="background: #7c4dff;"></div>
                      <div class="phone-app-icon" style="background: #10b981;"></div>
                      <div class="phone-app-icon" style="background: #6b7280;"></div>
                    </div>
                  </div>
                )}
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Featured Projects Section -->
  <section class="projects-section">
    <div class="section-container">
      <div class="section-header">
        <h2 class="section-title">Proyectos Destacados</h2>
        <p class="section-subtitle">Soluciones reales con resultados medibles</p>
      </div>

      <div class="projects-grid">
        {featuredProjects.map((project, index) => (
          <div class="project-card" data-tilt>
            <div class="project-icon">
              {index === 0 ? '☁️' : index === 1 ? '🛒' : index === 2 ? '📊' : '💼'}
            </div>
            
            <div class="project-image-wrapper">
              {project.data.cover ? (
                <img 
                  src={project.data.cover} 
                  alt={project.data.title}
                  class="project-image"
                />
              ) : (
                <div class="project-image-placeholder">
                  <span class="placeholder-icon">🌐</span>
                </div>
              )}
              <div class="project-platform-icons">
                <span class="platform-icon">💻</span>
                <span class="platform-icon">📱</span>
              </div>
            </div>

            <div class="project-content">
              <h3 class="project-title">{project.data.title}</h3>
              
              <div class="project-problem-solution">
                <div class="problem-item">
                  <span class="problem-label">Problema:</span>
                  <span class="problem-text">Gestión desactualizada en App móvil</span>
                </div>
                <div class="solution-item">
                  <span class="solution-label">Solución:</span>
                  <span class="solution-text">Sistema completo +40% usuarios activos</span>
                </div>
              </div>
            </div>

            <div class="project-footer">
              <a href={`/projects/${project.slug}`} class="project-link">
                Ver Proyecto
                <svg class="link-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
                </svg>
              </a>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- Success Cases Section -->
  <section class="success-section">
    <div class="section-container">
      <div class="section-header">
        <h2 class="section-title">Casos de Éxito</h2>
        <p class="section-subtitle">Resultados reales de mis clientes</p>
      </div>

      <div class="success-grid">
        <div class="success-steps">
          <div class="step-item">
            <div class="step-icon">📄</div>
            <span class="step-text">El problema</span>
          </div>
          <div class="step-item">
            <div class="step-icon">👥</div>
            <span class="step-text">El problema</span>
          </div>
          <div class="step-item">
            <div class="step-icon">💡</div>
            <span class="step-text">La solución</span>
          </div>
          <div class="step-item">
            <div class="step-icon">⚙️</div>
            <span class="step-text">La solución</span>
          </div>
          <div class="step-item">
            <div class="step-icon">✅</div>
            <span class="step-text">La solución</span>
          </div>
          <div class="step-item">
            <div class="step-icon">📊</div>
            <span class="step-text">Resultado / impacto</span>
          </div>
        </div>

        <div class="testimonials-grid">
          {testimonials.map((testimonial, index) => (
            <div class="testimonial-card" data-tilt>
              <div class="testimonial-phone-mockup">
                <div class="testimonial-phone-frame">
                  <div class="testimonial-phone-screen">
                    {index === 0 && featuredProjects[0] && (
                      <img 
                        src={featuredProjects[0].data.cover || "/images/projects/tienda-online/cover.svg"} 
                        alt="App Preview"
                        class="testimonial-app-preview"
                      />
                    )}
                    {index === 1 && featuredProjects[1] && (
                      <img 
                        src={featuredProjects[1].data.cover || "/images/projects/landing-barberia/cover.svg"} 
                        alt="App Preview"
                        class="testimonial-app-preview"
                      />
                    )}
                    {index === 2 && featuredProjects[0] && (
                      <img 
                        src={featuredProjects[0].data.cover || "/images/projects/tienda-online/cover.svg"} 
                        alt="App Preview"
                        class="testimonial-app-preview"
                      />
                    )}
                    {index === 3 && featuredProjects[1] && (
                      <img 
                        src={featuredProjects[1].data.cover || "/images/projects/landing-barberia/cover.svg"} 
                        alt="App Preview"
                        class="testimonial-app-preview"
                      />
                    )}
                  </div>
                </div>
              </div>
              
              <div class="testimonial-rating">
                {Array.from({ length: testimonial.rating }).map(() => (
                  <span class="testimonial-star">⭐</span>
                ))}
              </div>
              
              <div class="testimonial-content">
                <p class="testimonial-text">{testimonial.text}</p>
              </div>
              
              <div class="testimonial-author">
                <div class="testimonial-avatar">{testimonial.avatar}</div>
                <div class="testimonial-info">
                  <div class="testimonial-name">{testimonial.name}</div>
                  <div class="testimonial-role">{testimonial.role} - {testimonial.company}</div>
                </div>
              </div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="cta-section">
    <div class="section-container">
      <div class="cta-content">
        <h2 class="cta-title">¿Listo para impulsar tu negocio?</h2>
        <p class="cta-subtitle">Trabajemos juntos para crear soluciones digitales que generen resultados reales</p>
        <div class="cta-buttons">
          <a href="/contacto" class="btn btn-primary btn-large">
            Solicitar Propuesta
          </a>
          <a href="/projects" class="btn btn-secondary btn-large">
            Ver Todos los Proyectos
          </a>
        </div>
      </div>
    </div>
  </section>
</Layout>

<style>
  /* Hero Section */
  .hero-section {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    padding: 2rem 1rem;
    overflow: hidden;
    background: linear-gradient(135deg, #0a0e27 0%, #1a1f3a 50%, #0f172a 100%);
  }

  @media (max-width: 640px) {
    .hero-section {
      min-height: 100vh;
      padding: 3rem 1rem;
    }

    .hero-container {
      position: relative;
    }

    .hero-content {
      min-height: 80vh;
    }
  }

  .hero-background {
    position: absolute;
    inset: 0;
    background: 
      radial-gradient(circle at 20% 50%, rgba(59, 130, 246, 0.1) 0%, transparent 50%),
      radial-gradient(circle at 80% 80%, rgba(124, 77, 255, 0.1) 0%, transparent 50%);
    opacity: 0.6;
  }

  .hero-container {
    position: relative;
    z-index: 1;
    max-width: 1400px;
    margin: 0 auto;
    width: 100%;
  }

  .hero-content {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
    align-items: center;
    position: relative;
  }

  .hero-main {
    position: relative;
    z-index: 10;
    background: rgba(10, 14, 39, 0.3);
    backdrop-filter: blur(8px);
    padding: 2rem;
    border-radius: 20px;
    border: 1px solid rgba(255, 255, 255, 0.1);
    box-shadow: 0 8px 32px rgba(0, 0, 0, 0.2);
  }

  @media (min-width: 640px) {
    .hero-main {
      background: transparent;
      backdrop-filter: none;
      padding: 0;
      border: none;
      box-shadow: none;
    }
  }

  .hero-header {
    margin-bottom: 1rem;
  }

  .hero-logo {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .logo-icon {
    width: 40px;
    height: 40px;
    background: linear-gradient(135deg, #3b82f6, #7c4dff);
    border-radius: 10px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-weight: 700;
    font-size: 1.1rem;
  }

  .logo-text {
    color: white;
    font-size: 1.1rem;
    font-weight: 600;
  }

  .hero-title {
    font-size: clamp(1.75rem, 5vw, 3.5rem);
    font-weight: 800;
    line-height: 1.2;
    color: white;
    margin-bottom: 1.5rem;
  }

  @media (max-width: 640px) {
    .hero-title {
      font-size: 1.75rem;
      margin-bottom: 1rem;
    }
  }

  .gradient-text {
    background: linear-gradient(135deg, #60a5fa, #a78bfa);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
  }

  .hero-subtitle {
    font-size: clamp(1rem, 2vw, 1.25rem);
    color: rgba(255, 255, 255, 0.8);
    margin-bottom: 2rem;
    line-height: 1.6;
  }

  .hero-ratings {
    display: flex;
    align-items: center;
    gap: 1rem;
    margin-bottom: 2.5rem;
    flex-wrap: wrap;
  }

  .rating-stars {
    display: flex;
    gap: 0.25rem;
  }

  .star {
    font-size: 1.25rem;
  }

  .rating-text {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: rgba(255, 255, 255, 0.9);
    font-size: 0.95rem;
  }

  .rating-score {
    font-weight: 600;
    color: white;
  }

  .rating-separator {
    color: rgba(255, 255, 255, 0.5);
  }

  .rating-count {
    color: rgba(255, 255, 255, 0.7);
  }

  .hero-cta {
    display: flex;
    gap: 1rem;
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.875rem 1.75rem;
    border-radius: 12px;
    font-weight: 600;
    font-size: 1rem;
    text-decoration: none;
    transition: all 0.3s ease;
    border: none;
    cursor: pointer;
  }

  .btn-primary {
    background: linear-gradient(135deg, #3b82f6, #7c4dff);
    color: white;
    box-shadow: 0 4px 15px rgba(59, 130, 246, 0.3);
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(59, 130, 246, 0.4);
  }

  .btn-secondary {
    background: rgba(255, 255, 255, 0.1);
    color: white;
    border: 1px solid rgba(255, 255, 255, 0.2);
  }

  .btn-secondary:hover {
    background: rgba(255, 255, 255, 0.15);
    border-color: rgba(255, 255, 255, 0.3);
  }

  .btn-icon {
    width: 1.25rem;
    height: 1.25rem;
  }

  /* Device Mockups */
  .hero-mockups {
    display: block;
    position: absolute;
    inset: 0;
    pointer-events: none;
    z-index: 1;
    overflow: hidden;
  }

  .mockup-phone {
    position: absolute;
    opacity: 0.3;
    filter: blur(2px);
  }

  .mockup-phone-1 {
    top: 10%;
    right: -20%;
    width: 180px;
    height: 360px;
    transform: rotate(-15deg);
    z-index: 1;
  }

  .mockup-phone-2 {
    bottom: 10%;
    left: -15%;
    width: 160px;
    height: 320px;
    transform: rotate(15deg);
    z-index: 1;
  }

  @media (min-width: 640px) {
    .hero-mockups {
      position: relative;
      height: 400px;
      opacity: 1;
    }

    .mockup-phone {
      opacity: 1;
      filter: none;
    }

    .mockup-phone-1 {
      top: 0;
      right: auto;
      left: 0;
      width: 150px;
      height: 300px;
      transform: rotate(-8deg);
    }

    .mockup-phone-2 {
      bottom: 0;
      left: auto;
      right: 0;
      width: 150px;
      height: 300px;
      transform: rotate(8deg);
    }
  }

  .phone-frame {
    width: 100%;
    height: 100%;
    background: #1a1a1a;
    border-radius: 30px;
    padding: 8px;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.5);
  }

  @media (max-width: 640px) {
    .phone-frame {
      box-shadow: 0 10px 40px rgba(59, 130, 246, 0.2);
      border: 2px solid rgba(59, 130, 246, 0.3);
    }
  }

  .phone-frame {
    position: relative;
  }

  .phone-notch {
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 120px;
    height: 20px;
    background: #1a1a1a;
    border-radius: 0 0 15px 15px;
    z-index: 10;
  }

  .phone-screen {
    width: 100%;
    height: 100%;
    background: #000;
    border-radius: 22px;
    overflow: hidden;
    position: relative;
  }

  .phone-content {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .phone-placeholder {
    width: 100%;
    height: 100%;
    background: linear-gradient(135deg, #1a1f3a, #0f172a);
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 2rem;
  }

  .phone-app-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1rem;
    width: 100%;
  }

  .phone-app-icon {
    aspect-ratio: 1;
    border-radius: 12px;
    opacity: 0.8;
  }

  /* Projects Section */
  .projects-section {
    padding: 4rem 1rem;
    background: #0f172a;
  }

  @media (max-width: 640px) {
    .projects-section {
      padding: 3rem 1rem;
    }
  }

  .section-container {
    max-width: 1200px;
    margin: 0 auto;
  }

  .section-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  .section-title {
    font-size: clamp(2rem, 4vw, 2.5rem);
    font-weight: 700;
    color: white;
    margin-bottom: 1rem;
  }

  .section-subtitle {
    font-size: 1.1rem;
    color: rgba(255, 255, 255, 0.7);
  }

  .projects-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }

  @media (min-width: 640px) {
    .projects-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .project-card {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 20px;
    padding: 1.5rem;
    transition: all 0.3s ease;
  }

  .project-card:hover {
    transform: translateY(-5px);
    border-color: rgba(59, 130, 246, 0.3);
    box-shadow: 0 10px 30px rgba(59, 130, 246, 0.2);
  }

  .project-icon {
    font-size: 2rem;
    margin-bottom: 1rem;
  }

  .project-image-wrapper {
    position: relative;
    width: 100%;
    height: 200px;
    border-radius: 12px;
    overflow: hidden;
    margin-bottom: 1rem;
    background: rgba(255, 255, 255, 0.05);
  }

  @media (min-width: 768px) {
    .project-image-wrapper {
      height: 180px;
    }
  }

  .project-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .project-image-placeholder {
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, rgba(59, 130, 246, 0.2), rgba(124, 77, 255, 0.2));
  }

  .placeholder-icon {
    font-size: 3rem;
  }

  .project-platform-icons {
    position: absolute;
    bottom: 0.75rem;
    right: 0.75rem;
    display: flex;
    gap: 0.5rem;
  }

  .platform-icon {
    width: 32px;
    height: 32px;
    background: rgba(0, 0, 0, 0.6);
    border-radius: 8px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1rem;
    backdrop-filter: blur(10px);
  }

  .project-title {
    font-size: 1.25rem;
    font-weight: 600;
    color: white;
    margin-bottom: 1rem;
  }

  .project-problem-solution {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    margin-bottom: 1rem;
  }

  .problem-item,
  .solution-item {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .problem-label,
  .solution-label {
    font-size: 0.75rem;
    color: rgba(255, 255, 255, 0.6);
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  .problem-text,
  .solution-text {
    font-size: 0.9rem;
    color: rgba(255, 255, 255, 0.9);
  }

  .solution-text {
    color: #60a5fa;
  }

  .project-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: #60a5fa;
    text-decoration: none;
    font-weight: 500;
    transition: all 0.2s ease;
  }

  .project-link:hover {
    color: #3b82f6;
    gap: 0.75rem;
  }

  .link-icon {
    width: 1rem;
    height: 1rem;
  }

  /* Success Section */
  .success-section {
    padding: 4rem 1rem;
    background: #020617;
  }

  @media (max-width: 640px) {
    .success-section {
      padding: 3rem 1rem;
    }
  }

  .success-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 3rem;
  }

  .success-steps {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
    gap: 1rem;
    background: linear-gradient(135deg, rgba(59, 130, 246, 0.1), rgba(124, 77, 255, 0.1));
    padding: 2rem;
    border-radius: 20px;
  }

  .step-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    text-align: center;
  }

  .step-icon {
    font-size: 2rem;
    margin-bottom: 0.5rem;
  }

  .step-text {
    color: rgba(255, 255, 255, 0.9);
    font-size: 0.9rem;
  }

  .testimonials-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1.5rem;
  }

  @media (min-width: 640px) {
    .testimonials-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  .testimonial-card {
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 20px;
    padding: 1.5rem;
    transition: all 0.3s ease;
  }

  .testimonial-card:hover {
    transform: translateY(-5px);
    border-color: rgba(59, 130, 246, 0.3);
  }

  .testimonial-phone-mockup {
    width: 100%;
    height: 150px;
    margin-bottom: 1rem;
    display: flex;
    justify-content: center;
  }

  .testimonial-phone-frame {
    width: 80px;
    height: 150px;
    background: #1a1a1a;
    border-radius: 15px;
    padding: 4px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
  }

  .testimonial-phone-screen {
    width: 100%;
    height: 100%;
    background: #000;
    border-radius: 11px;
    overflow: hidden;
  }

  .testimonial-app-preview {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .testimonial-rating {
    display: flex;
    gap: 0.25rem;
    margin-bottom: 1rem;
  }

  .testimonial-star {
    font-size: 1rem;
  }

  .testimonial-text {
    color: rgba(255, 255, 255, 0.9);
    font-size: 0.9rem;
    line-height: 1.6;
    margin-bottom: 1rem;
  }

  .testimonial-author {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .testimonial-avatar {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    background: rgba(59, 130, 246, 0.2);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
  }

  .testimonial-name {
    color: white;
    font-weight: 600;
    font-size: 0.95rem;
  }

  .testimonial-role {
    color: rgba(255, 255, 255, 0.6);
    font-size: 0.8rem;
  }

  /* CTA Section */
  .cta-section {
    padding: 5rem 1rem;
    background: linear-gradient(135deg, #0f172a, #1a1f3a);
  }

  .cta-content {
    max-width: 800px;
    margin: 0 auto;
    text-align: center;
  }

  .cta-title {
    font-size: clamp(2rem, 4vw, 2.5rem);
    font-weight: 700;
    color: white;
    margin-bottom: 1rem;
  }

  .cta-subtitle {
    font-size: 1.1rem;
    color: rgba(255, 255, 255, 0.7);
    margin-bottom: 2.5rem;
  }

  .cta-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  .btn-large {
    padding: 1rem 2rem;
    font-size: 1.1rem;
  }

  /* Mobile Responsive - Mockups visible but subtle */
  @media (max-width: 639px) {
    .hero-mockups {
      opacity: 0.5;
    }

    .mockup-phone {
      animation: float 6s ease-in-out infinite;
      filter: blur(1px);
    }

    .mockup-phone-1 {
      animation-delay: 0s;
      right: -10%;
      top: 15%;
    }

    .mockup-phone-2 {
      animation-delay: 3s;
      animation-name: float2;
      left: -10%;
      bottom: 15%;
    }
  }

  @keyframes float {
    0%, 100% {
      transform: translateY(0) rotate(-15deg);
    }
    50% {
      transform: translateY(-20px) rotate(-12deg);
    }
  }

  @keyframes float2 {
    0%, 100% {
      transform: translateY(0) rotate(15deg);
    }
    50% {
      transform: translateY(-20px) rotate(18deg);
    }
  }

  @media (min-width: 640px) {
    .hero-mockups {
      display: flex;
      align-items: center;
      justify-content: center;
      height: 400px;
      margin-top: 2rem;
      gap: 1rem;
      position: relative;
      opacity: 1;
    }

    .mockup-phone {
      width: 150px;
      height: 300px;
      position: relative;
      opacity: 1;
      filter: none;
      animation: none;
    }

    .mockup-phone-1 {
      transform: rotate(-8deg);
    }

    .mockup-phone-2 {
      transform: rotate(8deg);
    }

    .projects-grid {
      grid-template-columns: repeat(2, 1fr);
    }

    .testimonials-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 768px) {
    .hero-content {
      grid-template-columns: 1fr 1fr;
    }

    .hero-mockups {
      height: 500px;
      margin-top: 0;
      position: relative;
    }

    .mockup-phone {
      width: 200px;
      height: 400px;
      position: absolute;
    }

    .mockup-phone-1 {
      top: 0;
      left: 0;
      transform: rotate(-5deg);
    }

    .mockup-phone-2 {
      bottom: 0;
      right: 0;
      transform: rotate(5deg);
    }

    .success-grid {
      grid-template-columns: 1fr 2fr;
    }

    .success-steps {
      grid-template-columns: 1fr;
    }

    .projects-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1024px) {
    .hero-mockups {
      height: 600px;
    }

    .mockup-phone {
      width: 250px;
      height: 500px;
    }

    .projects-grid {
      grid-template-columns: repeat(4, 1fr);
    }

    .testimonials-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (min-width: 1280px) {
    .testimonials-grid {
      grid-template-columns: repeat(4, 1fr);
    }
  }

  /* Animations */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(30px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .project-card,
  .testimonial-card {
    animation: fadeInUp 0.6s ease-out;
  }

  .project-card:nth-child(1) { animation-delay: 0.1s; }
  .project-card:nth-child(2) { animation-delay: 0.2s; }
  .project-card:nth-child(3) { animation-delay: 0.3s; }
  .project-card:nth-child(4) { animation-delay: 0.4s; }
</style>

<script>
  import { parallax } from "../scripts/parallax.js";
  import { revealOnScroll } from "../scripts/reveal.js";
  
  parallax();
  revealOnScroll();
</script>