---
import Layout from '../layouts/Layout.astro';
import Button from '../components/Button.astro';
import { SERVICES, SERVICE_CATEGORIES, type Service, type ServiceCategory } from '../utils/services';
---

<Layout 
  title="Servicios de Diseño Web | Diseñador de Páginas Web, Apps, Marca y Menús QR | Jonathan Guarirapa"
  description="Servicios profesionales de diseño web: diseño de páginas web, diseño de apps web, diseño de marca, diseño de logos, menús QR para restaurantes, sistemas de gestión y más. Soluciones completas para tu negocio digital."
>
  <!-- Structured Data for Services -->
  <script type="application/ld+json" set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "Service",
    "serviceType": "Diseño Web y Desarrollo",
    "provider": {
      "@type": "Person",
      "name": "Jonathan Guarirapa",
      "email": "jona.develp@gmail.com"
    },
    "areaServed": {
      "@type": "Country",
      "name": "Chile"
    },
    "hasOfferCatalog": {
      "@type": "OfferCatalog",
      "name": "Servicios de Diseño Web",
      "itemListElement": [
        {
          "@type": "Offer",
          "itemOffered": {
            "@type": "Service",
            "name": "Diseño de Páginas Web",
            "description": "Diseño profesional de páginas web modernas, responsivas y optimizadas para SEO"
          }
        },
        {
          "@type": "Offer",
          "itemOffered": {
            "@type": "Service",
            "name": "Diseño de Apps Web",
            "description": "Desarrollo de aplicaciones web personalizadas con tecnologías modernas"
          }
        },
        {
          "@type": "Offer",
          "itemOffered": {
            "@type": "Service",
            "name": "Diseño de Marca y Logo",
            "description": "Creación de identidad visual completa, diseño de logos y branding"
          }
        },
        {
          "@type": "Offer",
          "itemOffered": {
            "@type": "Service",
            "name": "Menús QR para Restaurantes",
            "description": "Sistemas de menús digitales interactivos con código QR para restaurantes"
          }
        },
        {
          "@type": "Offer",
          "itemOffered": {
            "@type": "Service",
            "name": "Sistemas de Gestión",
            "description": "Desarrollo de sistemas de gestión personalizados para empresas"
          }
        },
        {
          "@type": "Offer",
          "itemOffered": {
            "@type": "Service",
            "name": "Creación de Imágenes",
            "description": "Diseño y creación de imágenes profesionales para web y marketing"
          }
        }
      ]
    }
  })} />
  <!-- Hero Section -->
  <section class="relative min-h-[80vh] flex items-center justify-center overflow-hidden">
    <!-- Background -->
    <div class="absolute inset-0 bg-gradient-to-br from-nextsite-bg-darker via-nextsite-dark-blue to-nextsite-bg-dark"></div>
    
    <!-- Animated Elements -->
    <div class="absolute top-20 left-10 w-32 h-32 bg-gradient-to-r from-cyan-400 to-blue-500 rounded-full blur-2xl animate-float opacity-20"></div>
    <div class="absolute bottom-20 right-10 w-40 h-40 bg-gradient-to-r from-blue-500 to-cyan-400 rounded-full blur-2xl animate-float opacity-15" style="animation-delay: 2s;"></div>
    
    <div class="relative z-10 max-w-6xl mx-auto px-4 text-center">
      <div class="animate-fade-in-up">
        <h1 class="text-5xl md:text-7xl font-bold mb-6">
          <span class="text-white">Servicios de</span>
          <br>
          <span class="bg-gradient-to-r from-cyan-400 to-blue-500 bg-clip-text text-transparent">
            Diseño Web
          </span>
        </h1>
        <p class="text-xl md:text-2xl text-gray-300 mb-8 max-w-3xl mx-auto leading-relaxed">
          Sistemas de gestión con hardware, menús QR y pedidos digitales, marketing digital y presencia online. Soluciones completas que automatizan tu negocio y generan resultados.
        </p>
        <div class="flex flex-col sm:flex-row gap-4 justify-center">
          <Button href="/contacto" size="lg">
            Solicitar Cotización
          </Button>
          <Button href="#servicios-categorias" variant="ghost" size="lg">
            Ver Todos los Servicios
          </Button>
        </div>
      </div>
    </div>
  </section>

  <!-- Services by Category -->
  <section id="servicios-categorias" class="py-20 bg-nextsite-bg-dark">
    <div class="max-w-7xl mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-4xl font-bold text-white mb-4">
          Servicios por <span class="text-nextsite-accent-blue">Categoría</span>
        </h2>
        <p class="text-xl text-gray-400 max-w-3xl mx-auto">
          Explora todos nuestros servicios organizados por categorías. Selecciona los que necesitas para tu negocio.
        </p>
      </div>
      
      <!-- Categories Tabs (Desktop) - Diseño más grande y visual -->
      <div class="hidden md:flex justify-center gap-6 mb-16">
        {SERVICE_CATEGORIES.map((category, index) => (
          <button
            class="category-tab-large"
            data-category={category.id}
            style={`
              padding: 1.5rem 2.5rem;
              background: ${index === 0 ? 'linear-gradient(135deg, #1e40af 0%, #3b82f6 100%)' : 'rgba(255, 255, 255, 0.05)'};
              border: 2px solid ${index === 0 ? '#3b82f6' : 'rgba(255, 255, 255, 0.1)'};
              border-radius: 20px;
              color: ${index === 0 ? '#FFFFFF' : '#E5E7EB'};
              font-weight: 700;
              font-size: 1.125rem;
              cursor: pointer;
              transition: all 0.3s ease;
              display: flex;
              flex-direction: column;
              align-items: center;
              gap: 0.75rem;
              min-width: 200px;
              box-shadow: ${index === 0 ? '0 8px 24px rgba(59, 130, 246, 0.3)' : '0 4px 12px rgba(0, 0, 0, 0.1)'};
            `}
            onmouseover="if (!this.classList.contains('active')) { 
              this.style.background = 'rgba(59, 130, 246, 0.2)'; 
              this.style.borderColor = '#3b82f6'; 
              this.style.transform = 'translateY(-4px)';
              this.style.boxShadow = '0 8px 24px rgba(59, 130, 246, 0.2)';
            }"
            onmouseout="if (!this.classList.contains('active')) { 
              this.style.background = 'rgba(255, 255, 255, 0.05)'; 
              this.style.borderColor = 'rgba(255, 255, 255, 0.1)'; 
              this.style.transform = 'translateY(0)';
              this.style.boxShadow = '0 4px 12px rgba(0, 0, 0, 0.1)';
            }"
            onclick={`
              document.querySelectorAll('.category-tab-large').forEach(t => {
                t.classList.remove('active');
                t.style.background = 'rgba(255, 255, 255, 0.05)';
                t.style.borderColor = 'rgba(255, 255, 255, 0.1)';
                t.style.color = '#E5E7EB';
                t.style.boxShadow = '0 4px 12px rgba(0, 0, 0, 0.1)';
              });
              this.classList.add('active');
              this.style.background = 'linear-gradient(135deg, #1e40af 0%, #3b82f6 100%)';
              this.style.borderColor = '#3b82f6';
              this.style.color = '#FFFFFF';
              this.style.boxShadow = '0 8px 24px rgba(59, 130, 246, 0.3)';
              document.querySelectorAll('.service-category-section').forEach(s => s.style.display = 'none');
              document.getElementById('category-${category.id}').style.display = 'block';
            `}
          >
            <span style="font-size: 3rem; line-height: 1;">{category.icon}</span>
            <span>{category.name}</span>
          </button>
        ))}
          </div>
          
      <!-- Mobile Category Selector -->
      <div class="md:hidden mb-8">
        <select
          id="mobile-category-selector"
          class="w-full p-4 bg-gray-800 border-2 border-gray-700 rounded-xl text-white text-lg font-semibold"
          style="background: #1F2937; border-color: #4B5563;"
          onchange={`
            document.querySelectorAll('.service-category-section').forEach(s => s.style.display = 'none');
            const selected = this.value;
            if (selected) {
              document.getElementById('category-' + selected).style.display = 'block';
            }
          `}
        >
          <option value="">Selecciona una categoría</option>
          {SERVICE_CATEGORIES.map((category) => (
            <option value={category.id}>{category.icon} {category.name}</option>
          ))}
        </select>
      </div>

      <!-- Services by Category -->
      {SERVICE_CATEGORIES.map((category, categoryIndex) => {
        const categoryServices = SERVICES[category.id];
        return (
          <div
            id={`category-${category.id}`}
            class="service-category-section"
            style={`display: ${categoryIndex === 0 ? 'block' : 'none'};`}
          >
            <!-- Category Header - Mejorado y más visual -->
            <div class="text-center mb-16">
              <div class="inline-flex flex-col items-center gap-4 mb-6">
                <div style="
                  width: 100px;
                  height: 100px;
                  border-radius: 24px;
                  background: linear-gradient(135deg, #3b82f6 0%, #60a5fa 100%);
                  display: flex;
                  align-items: center;
                  justify-content: center;
                  font-size: 3.5rem;
                  box-shadow: 0 12px 32px rgba(59, 130, 246, 0.4);
                  margin-bottom: 1rem;
                ">
                  {category.icon}
                </div>
                <div>
                  <h3 class="text-4xl font-bold text-white mb-3">{category.name}</h3>
                  <p class="text-xl text-gray-300 max-w-2xl mx-auto leading-relaxed">{category.description}</p>
                </div>
              </div>
            </div>
          
            <!-- Services Grid - Diseño más grande y destacado -->
            <div class="grid md:grid-cols-2 lg:grid-cols-3 gap-8 mb-16">
              {categoryServices.map((service, serviceIndex) => (
                <div
                  class="service-card-enhanced glass rounded-3xl p-8 hover:bg-white/10 transition-all duration-300 group"
                  style={`
                    animation-delay: ${serviceIndex * 0.1}s;
                    border: 2px solid ${service.popular ? 'rgba(59, 130, 246, 0.3)' : 'rgba(255, 255, 255, 0.1)'};
                    position: relative;
                    min-height: 420px;
                    display: flex;
                    flex-direction: column;
                    background: ${service.popular ? 'linear-gradient(135deg, rgba(59, 130, 246, 0.1) 0%, rgba(147, 197, 253, 0.05) 100%)' : 'rgba(255, 255, 255, 0.03)'};
                    box-shadow: ${service.popular ? '0 8px 32px rgba(59, 130, 246, 0.2)' : '0 4px 16px rgba(0, 0, 0, 0.1)'};
                  `}
                  onmouseenter="this.style.transform = 'translateY(-8px) scale(1.02)'; this.style.boxShadow = '0 16px 48px rgba(59, 130, 246, 0.3)'; this.style.borderColor = '#3b82f6';"
                  onmouseleave={`this.style.transform = 'translateY(0) scale(1)'; const isPopular = ${service.popular}; this.style.boxShadow = isPopular ? '0 8px 32px rgba(59, 130, 246, 0.2)' : '0 4px 16px rgba(0, 0, 0, 0.1)'; this.style.borderColor = isPopular ? 'rgba(59, 130, 246, 0.3)' : 'rgba(255, 255, 255, 0.1)';`}
                >
                  {service.popular && (
                    <span
                      style="
                        position: absolute;
                        top: 1.5rem;
                        right: 1.5rem;
                        background: linear-gradient(90deg, #00C2FF, #7C4DFF);
                        color: #051018;
                        padding: 0.5rem 1rem;
                        border-radius: 999px;
                        font-size: 0.875rem;
                        font-weight: 700;
                        z-index: 10;
                        box-shadow: 0 4px 12px rgba(0, 194, 255, 0.4);
                      "
                    >
                      ⭐ Popular
                    </span>
                  )}

                  <!-- Icono grande y destacado -->
                  <div class="mb-6 flex items-center justify-center">
                    <div 
                      class="w-20 h-20 rounded-2xl flex items-center justify-center"
                      style="
                        background: linear-gradient(135deg, #3b82f6 0%, #60a5fa 100%);
                        font-size: 2.5rem;
                        box-shadow: 0 8px 24px rgba(59, 130, 246, 0.3);
                      "
                    >
                      {service.icon || category.icon || '⚙️'}
                    </div>
                  </div>
          
                  <div class="mb-6 flex-1">
                    <h4
                      class="text-2xl font-bold text-white mb-4"
                      style="line-height: 1.3; min-height: 3.5rem;"
                    >
                      {service.name}
                    </h4>
                    <p class="text-gray-300 text-base leading-relaxed mb-6" style="min-height: 3.5rem;">
                      {service.description}
                    </p>

                    {service.features && service.features.length > 0 && (
                      <div class="mb-6">
                        <ul class="text-gray-300 space-y-3 text-sm">
                          {service.features.slice(0, 4).map((feature) => (
                            <li class="flex items-start gap-3">
                              <span class="text-nextsite-accent-blue mt-0.5 font-bold text-lg flex-shrink-0">✓</span>
                              <span class="flex-1">{feature}</span>
                            </li>
                          ))}
                          {service.features.length > 4 && (
                            <li class="text-gray-400 text-sm font-semibold pt-2">
                              +{service.features.length - 4} características más
                            </li>
                          )}
                        </ul>
                      </div>
                    )}

                    {service.includesHardware && (
                      <div
                        class="mb-6 p-4 rounded-xl"
                        style="background: rgba(59, 130, 246, 0.15); border: 2px solid rgba(59, 130, 246, 0.4);"
                      >
                        <p class="text-blue-300 text-sm font-bold flex items-center gap-2">
                          <span style="font-size: 1.25rem;">📦</span>
                          <span>Incluye hardware completo</span>
                        </p>
                      </div>
                    )}
                  </div>
        
                  <div class="w-full mt-auto">
                    <a
                      href={`/contacto?service=${service.id}`}
                      class="block w-full text-center px-8 py-4 rounded-xl font-bold text-base transition-all duration-300 text-white"
                      style="
                        background: linear-gradient(135deg, #3b82f6 0%, #60a5fa 100%);
                        border: none;
                        box-shadow: 0 4px 16px rgba(59, 130, 246, 0.4);
                      "
                      onmouseenter="this.style.background = 'linear-gradient(135deg, #2563eb 0%, #3b82f6 100%)'; this.style.boxShadow = '0 6px 20px rgba(59, 130, 246, 0.5)'; this.style.transform = 'translateY(-2px)';"
                      onmouseleave="this.style.background = 'linear-gradient(135deg, #3b82f6 0%, #60a5fa 100%)'; this.style.boxShadow = '0 4px 16px rgba(59, 130, 246, 0.4)'; this.style.transform = 'translateY(0)';"
                    >
                      Solicitar {service.name.split('(')[0].trim()}
                    </a>
                  </div>
                </div>
              ))}
            </div>
          </div>
        );
      })}
    </div>
  </section>

  <!-- Quick CTA -->
  <section class="py-16 bg-gradient-to-r from-cyan-500/10 to-blue-500/10">
    <div class="max-w-4xl mx-auto px-4 text-center">
      <h3 class="text-2xl font-bold text-white mb-4">
        ¿No encuentras lo que buscas?
      </h3>
      <p class="text-gray-300 mb-6">
        Cada negocio es único. Contáctame para una solución personalizada.
      </p>
      <Button href="/contacto" size="lg">
        Solicitar Consulta Personalizada
          </Button>
    </div>
  </section>

  <!-- Process Section -->
  <section class="py-20">
    <div class="max-w-6xl mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-4xl font-bold text-white mb-4">
          Mi <span class="text-nextsite-accent-blue">Proceso</span> de Trabajo
        </h2>
        <p class="text-xl text-gray-400 max-w-3xl mx-auto">
          Un proceso estructurado que garantiza resultados excepcionales en cada proyecto
        </p>
      </div>
      
      <div class="grid md:grid-cols-4 gap-8">
        <!-- Step 1 -->
        <div class="text-center animate-fade-in-up">
          <div class="w-20 h-20 bg-gradient-to-r from-cyan-400 to-blue-500 rounded-2xl flex items-center justify-center mx-auto mb-6">
            <span class="text-2xl font-bold text-white">1</span>
          </div>
          <h3 class="text-xl font-bold text-white mb-4">Consulta Inicial</h3>
          <p class="text-gray-400 leading-relaxed">
            Analizamos tus necesidades, objetivos y presupuesto para crear la mejor estrategia.
          </p>
        </div>
        
        <!-- Step 2 -->
        <div class="text-center animate-fade-in-up" style="animation-delay: 0.2s;">
          <div class="w-20 h-20 bg-gradient-to-r from-blue-500 to-cyan-400 rounded-2xl flex items-center justify-center mx-auto mb-6">
            <span class="text-2xl font-bold text-white">2</span>
          </div>
          <h3 class="text-xl font-bold text-white mb-4">Propuesta</h3>
          <p class="text-gray-400 leading-relaxed">
            Te presento una propuesta detallada con timeline, costos y especificaciones técnicas.
          </p>
        </div>
        
        <!-- Step 3 -->
        <div class="text-center animate-fade-in-up" style="animation-delay: 0.4s;">
          <div class="w-20 h-20 bg-gradient-to-r from-cyan-400 to-blue-500 rounded-2xl flex items-center justify-center mx-auto mb-6">
            <span class="text-2xl font-bold text-white">3</span>
          </div>
          <h3 class="text-xl font-bold text-white mb-4">Desarrollo</h3>
          <p class="text-gray-400 leading-relaxed">
            Desarrollo tu proyecto con comunicación constante y actualizaciones regulares.
          </p>
        </div>
        
        <!-- Step 4 -->
        <div class="text-center animate-fade-in-up" style="animation-delay: 0.6s;">
          <div class="w-20 h-20 bg-gradient-to-r from-blue-500 to-cyan-400 rounded-2xl flex items-center justify-center mx-auto mb-6">
            <span class="text-2xl font-bold text-white">4</span>
          </div>
          <h3 class="text-xl font-bold text-white mb-4">Entrega</h3>
          <p class="text-gray-400 leading-relaxed">
            Entregamos tu proyecto optimizado y listo para lanzar, con soporte continuo.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Pricing Section -->
  <section class="py-20 bg-nextsite-bg-dark">
    <div class="max-w-6xl mx-auto px-4">
      <div class="text-center mb-16">
        <h2 class="text-4xl font-bold text-white mb-4">
          <span class="text-nextsite-accent-blue">Precios</span> Transparentes
        </h2>
        <p class="text-xl text-gray-400 max-w-3xl mx-auto">
          Precios justos y transparentes para cada tipo de proyecto
        </p>
      </div>
      
      <div class="grid md:grid-cols-3 gap-8">
        <!-- Basic Package -->
        <div class="glass rounded-2xl p-8 hover:bg-white/10 transition-all duration-300 animate-fade-in-up">
          <h3 class="text-2xl font-bold text-white mb-4">Landing Page</h3>
          <div class="text-4xl font-bold text-nextsite-accent-blue mb-6">$70.000 CLP</div>
          <ul class="text-gray-300 space-y-3 mb-8">
            <li class="flex items-center gap-2">
              <span class="text-nextsite-accent-blue">✓</span>
              Diseño responsive
            </li>
            <li class="flex items-center gap-2">
              <span class="text-nextsite-accent-blue">✓</span>
              Optimización SEO básica
            </li>
            <li class="flex items-center gap-2">
              <span class="text-nextsite-accent-blue">✓</span>
              Formulario de contacto
            </li>
            <li class="flex items-center gap-2">
              <span class="text-nextsite-accent-blue">✓</span>
              Hasta 5 secciones
            </li>
            <li class="flex items-center gap-2">
              <span class="text-nextsite-accent-blue">✓</span>
              Entrega en 1 semana
            </li>
          </ul>
          <Button href="/contacto" variant="ghost" size="lg" class="w-full">
            Solicitar Landing Page
          </Button>
        </div>
        
        <!-- Professional Package -->
        <div class="glass rounded-2xl p-8 hover:bg-white/10 transition-all duration-300 animate-fade-in-up border-2 border-nextsite-accent-blue relative" style="animation-delay: 0.2s;">
          <div class="absolute -top-4 left-1/2 transform -translate-x-1/2">
            <span class="bg-nextsite-accent text-white px-4 py-1 rounded-full text-sm font-semibold">Más Popular</span>
          </div>
          <h3 class="text-2xl font-bold text-white mb-4">Sitio Web Completo</h3>
          <div class="text-4xl font-bold text-nextsite-accent-blue mb-6">$120.000 CLP</div>
          <ul class="text-gray-300 space-y-3 mb-8">
            <li class="flex items-center gap-2">
              <span class="text-nextsite-accent-blue">✓</span>
              Hasta 10 páginas
            </li>
            <li class="flex items-center gap-2">
              <span class="text-nextsite-accent-blue">✓</span>
              Diseño personalizado
            </li>
            <li class="flex items-center gap-2">
              <span class="text-nextsite-accent-blue">✓</span>
              SEO optimizado
            </li>
            <li class="flex items-center gap-2">
              <span class="text-nextsite-accent-blue">✓</span>
              Panel de administración
            </li>
            <li class="flex items-center gap-2">
              <span class="text-nextsite-accent-blue">✓</span>
              Entrega en 2-3 semanas
            </li>
          </ul>
          <Button href="/contacto" size="lg" class="w-full">
            Solicitar Sitio Web
          </Button>
        </div>
        
        <!-- Enterprise Package -->
        <div class="glass rounded-2xl p-8 hover:bg-white/10 transition-all duration-300 animate-fade-in-up" style="animation-delay: 0.4s;">
          <h3 class="text-2xl font-bold text-white mb-4">Aplicación Web</h3>
          <div class="text-4xl font-bold text-nextsite-accent-blue mb-6">$200.000+ CLP</div>
          <ul class="text-gray-300 space-y-3 mb-8">
            <li class="flex items-center gap-2">
              <span class="text-nextsite-accent-blue">✓</span>
              Desarrollo personalizado
            </li>
            <li class="flex items-center gap-2">
              <span class="text-nextsite-accent-blue">✓</span>
              Base de datos incluida
            </li>
            <li class="flex items-center gap-2">
              <span class="text-nextsite-accent-blue">✓</span>
              Autenticación de usuarios
            </li>
            <li class="flex items-center gap-2">
              <span class="text-nextsite-accent-blue">✓</span>
              API personalizada
            </li>
            <li class="flex items-center gap-2">
              <span class="text-nextsite-accent-blue">✓</span>
              Entrega en 4-6 semanas
            </li>
          </ul>
          <Button href="/contacto" variant="ghost" size="lg" class="w-full">
            Solicitar Aplicación
          </Button>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="py-20">
    <div class="max-w-4xl mx-auto px-4 text-center">
      <div class="animate-fade-in-up">
        <h2 class="text-4xl md:text-5xl font-bold text-white mb-6">
          ¿Listo para <span class="text-nextsite-accent-blue">Empezar</span> tu Proyecto?
        </h2>
        <p class="text-xl text-gray-400 mb-12 max-w-3xl mx-auto leading-relaxed">
          Contáctame hoy mismo para discutir tu proyecto y recibir una cotización personalizada
        </p>
        
        <div class="flex flex-col sm:flex-row gap-6 justify-center">
          <Button href="/contacto" size="lg" class="animate-pulse-glow">
            <span class="flex items-center gap-2">
              Solicitar Cotización
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6"></path>
              </svg>
            </span>
          </Button>
          <Button href="/proyectos" variant="ghost" size="lg">
            <span class="flex items-center gap-2">
              Ver Mis Trabajos
              <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 12a3 3 0 11-6 0 3 3 0 016 0z"></path>
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M2.458 12C3.732 7.943 7.523 5 12 5c4.478 0 8.268 2.943 9.542 7-1.274 4.057-5.064 7-9.542 7-4.477 0-8.268-2.943-9.542-7z"></path>
              </svg>
            </span>
          </Button>
        </div>
      </div>
    </div>
  </section>
</Layout>