{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm ci && npm run build && ls -la dist/ && test -f dist/src/main.js || (echo 'Build failed - dist/src/main.js not found' && exit 1)"
  },
  "deploy": {
    "startCommand": "node dist/src/main",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
